{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Swift Package",
            "type": "shell",
            "command": "swift run",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
